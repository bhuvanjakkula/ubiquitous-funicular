import test from'node:test';import assert from'node:assert/strict';import{gox}from'../src/platform.js';test('identity/compliance',()=>{const b=gox.identity.register({country:'IN'}),s=gox.identity.register({country:'US'});gox.identity.verify(b.id);gox.identity.verify(s.id);assert.equal(gox.compliance.evaluate({buyerVerified:true,sellerVerified:true}).allowed,true)});test('matching',()=>{try{gox.capTable.createShareClass({issuerId:'i',securityId:'S',name:'Legacy',authorized:100})}catch{};if(gox.capTable.balance('i','S','s')<7)gox.capTable.issue({issuerId:'i',securityId:'S',ownerId:'s',quantity:7});gox.liquidity.place({participantId:'b',issuerId:'i',securityId:'S',side:'BUY',quantity:10,priceMinor:120,autoMatch:false});gox.liquidity.place({participantId:'s',issuerId:'i',securityId:'S',side:'SELL',quantity:7,priceMinor:100,autoMatch:false});assert.equal(gox.liquidity.match('S')[0].quantity,7)});test('settlement',()=>{const s=gox.settlement.create('T');gox.settlement.confirm(s.id,'cash');assert.equal(s.status,'PENDING');gox.settlement.confirm(s.id,'asset');assert.equal(s.status,'SETTLED')});
