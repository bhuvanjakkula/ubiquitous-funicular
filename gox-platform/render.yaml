services:
  - type: web
    name: gox-platform
    env: node
    plan: free
    buildCommand: cd gox-platform && npm test
    startCommand: node gox-platform/src/server.js
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PORT
        value: 10000
