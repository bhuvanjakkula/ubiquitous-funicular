// Preview only: preserve quoted commas/newlines and escaped quotes; no money logic.
export function previewCsv(text:string):string[][] {const rows:string[][]=[];let row:string[]=[],value='',quoted=false;for(let i=0;i<text.length;i++){const c=text[i];if(c==='"'){if(quoted&&text[i+1]==='"'){value+='"';i++}else quoted=!quoted;}else if(c===','&&!quoted){row.push(value);value=''}else if((c==='\n'||c==='\r')&&!quoted){if(c==='\r'&&text[i+1]==='\n')i++;row.push(value);rows.push(row);row=[];value='';if(rows.length===6)return rows;}else value+=c;}if(value||row.length){row.push(value);rows.push(row)}return rows.slice(0,6);}
