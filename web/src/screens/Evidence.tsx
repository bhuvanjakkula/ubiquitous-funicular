import {DISCLAIMER,evidenceJsonUrl,evidencePdfUrl} from '../api';
export default function Evidence({jobId}:{jobId:string}){return <section><p className="eyebrow">03 / WORKPAPER</p><h1>Keep the evidence.</h1><p className="sub">Source citations, input hashes, and the cash roll-forward in one local workpaper.</p><p className="notice">{DISCLAIMER}</p><div className="evidence-links"><a className="button" href={evidencePdfUrl(jobId)} download>Download PDF</a><a className="button secondary" href={evidenceJsonUrl(jobId)} target="_blank" rel="noreferrer">Open JSON ↗</a></div><p>Professional review required. Findings are not journal entries.</p></section>}
