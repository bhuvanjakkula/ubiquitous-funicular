[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "scientific_operator"
version = "1.0.0"
description = "Scientific Stock Operator — Risk Codex"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
scientific_operator = "scientific_operator.cli:main"
