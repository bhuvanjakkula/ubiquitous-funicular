services:
  - type: web
    name: the-scientific-operator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn --app-dir web server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9

