export const CURRENCY_EXPONENTS={USD:2,EUR:2,GBP:2,JPY:0,BHD:3} as const;
export function parseMinor(value:string,currency:string){const exponent=CURRENCY_EXPONENTS[currency as keyof typeof CURRENCY_EXPONENTS];if(exponent===undefined)throw new Error(`Unsupported currency ${currency}`);let cleaned=value.trim().replace(new RegExp(currency,"ig"),"").replace(/[,$£€¥\s]/g,"");const parenthesized=/^\(.*\)$/.test(cleaned);if(parenthesized)cleaned=`-${cleaned.slice(1,-1)}`;if(!/^-?\d+(?:\.\d+)?$/.test(cleaned))throw new Error(`Invalid amount ${value}`);const [whole,fraction=""]=cleaned.split(".");if(fraction.length>exponent)throw new Error(`${currency} amount has too many decimal places`);const sign=whole.startsWith("-")?-1n:1n,absolute=whole.replace("-","");return sign*(BigInt(absolute)*10n**BigInt(exponent)+BigInt((fraction+"0".repeat(exponent)).slice(0,exponent)||"0"));}
