{
  "name": "gox-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node -e \"import('node:fs').then(fs => fs.cpSync('gox-platform/src/static', 'dist', {recursive: true}))\"",
    "start": "node gox-platform/src/server.js",
    "test": "node --test gox-platform/test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}