{
  "name": "the-stock-operator",
  "buildCommand": "mkdir -p dist && cp -r risk_pulse/static/* dist/",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://the-stock-operator.onrender.com/api/$1"
    }
  ]
}
