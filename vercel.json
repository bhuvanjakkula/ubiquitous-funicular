{
  "name": "the-scientific-operator",
  "buildCommand": "mkdir -p dist && cp -r web/static/. dist/",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://the-scientific-operator.onrender.com/api/$1"
    }
  ]
}
